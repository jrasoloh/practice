#include <unistd.h>
#include <stdio.h>

void		fprime(int n)
{

}

int			main(int argc, char **argv)
{
	if (argc != 2)
	{
		write(1, &c, 1);
		return (1);
	}
	else
		fprime(atoi(argv[1]));
	return (0);
}
